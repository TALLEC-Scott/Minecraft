#version 330 core
in vec2 TexCoord;
in vec4 Color;
out vec4 FragColor;

uniform sampler2D uiTexture;
uniform int useTexture;

void main() {
    if (useTexture == 1) {
        vec4 tex = texture(uiTexture, TexCoord);
        FragColor = tex * Color;
    } else {
        FragColor = Color;
    }
}
